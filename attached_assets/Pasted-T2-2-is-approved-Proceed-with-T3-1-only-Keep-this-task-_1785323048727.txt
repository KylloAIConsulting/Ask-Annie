T2.2 is approved.

Proceed with T3.1 only.

Keep this task credit-efficient:
- Do not restate the Sprint 2 plan.
- Do not provide architecture commentary unless a blocker is found.
- Do not begin T4.1 or any other task.
- Make only the changes required for T3.1.
- Run the relevant tests.
- Run the client typecheck.
- Commit the work.
- Stop.

T3.1 scope:

Create the WelcomeScreen component and integrate it into the existing App shell.

Add:
- client/src/screens/WelcomeScreen/WelcomeScreen.tsx
- client/src/screens/WelcomeScreen/WelcomeScreen.module.css
- client/src/__tests__/WelcomeScreen.test.tsx

Modify:
- client/src/App.tsx
- client/src/components/Header/Header.tsx only if required to connect the existing “How it works” control to the WelcomeScreen panel

Requirements:

Content must match the approved screen specification:

Heading:
“Not sure if a message is genuine?”

Supporting text:
“Ask Annie can help you spot warning signs and decide what to do next.”

Reassurance:
“You will not be judged. It is always sensible to check before you click, reply, pay or share information.”

Privacy message:
“Only share the information needed for the check. Remove personal details where possible.”

Primary action:
“Check a message”
- Calls onNavigate('submit')

Secondary action:
“How Ask Annie works”
- Toggles an expandable explanation section
- Must use a real button
- Must expose aria-expanded
- Must reference the controlled panel with aria-controls
- Do not use href="#" or a dead link

Explanation steps:
1. “Share the message”
2. “Annie checks for warning signs”
3. “Get clear advice on what to do next.”

Panel behaviour:
- Hidden from the DOM when closed, or otherwise implemented without leaving hidden focusable content
- Visible when expanded
- Keyboard accessible
- Header “How it works” control must navigate to WelcomeScreen if required and open the same panel through an explicit callback/state contract
- Do not duplicate separate “How it works” content in Header

Styling:
- Use existing CSS tokens only
- Primary button minimum height must use the existing button-height token
- No horizontal overflow at mobile width
- Calm, readable single-column layout
- No any types

Tests must cover:
- WelcomeScreen renders
- jest-axe passes when panel is closed
- jest-axe passes when panel is open
- “Check a message” calls onNavigate('submit')
- “How Ask Annie works” opens and closes the panel
- aria-expanded changes correctly
- aria-controls points to the correct panel
- all three explanation steps render when open
- Header “How it works” opens the WelcomeScreen panel correctly
- only one WelcomeScreen is rendered through App

Commit message:

feat(screen): add WelcomeScreen and how-it-works panel

Report only:

1. Files changed
2. Tests run and results
3. Client typecheck result
4. Commit hash
5. Working-tree status

Then stop and wait for approval.